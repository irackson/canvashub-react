import Nav from './Nav';

const Header = (props) => {
    return (
        <div>
            <h1 className="header__title">CanvasHub</h1>
            <Nav />
        </div>
    );
};

export default Header;
