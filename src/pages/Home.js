const Home = (props) => {
    return (
        <div>
            <h2>Welcome to CanvasHub!</h2>
            <h3>
                It's like GitHub, but for drawing repositories made on the HTML5
                Canvas!
            </h3>
        </div>
    );
};

export default Home;
